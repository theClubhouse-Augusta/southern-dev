section.schedule
  header
    h1 Schedule
    each event in events
      event.title
    div(class='event-display')
      h4= event.title
      h5= event.speaker
      h5= event.date
      p= event.description
      img(src='#{event.image}')
